# =============================================
# logic-layer/src/main/resources/application.properties
# Phiên b?n cu?i ? ??y ?? t?t c? c?u hình
# =============================================

spring.application.name=logic-layer

# --- Database ---
spring.datasource.url=jdbc:mariadb://localhost:3306/smartroomms
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect

# --- Server ---
server.port=8081

# =============================================
# JWT ? THAY jwt.secret tr??c khi deploy!
# T?o secret: openssl rand -base64 32
# =============================================
jwt.secret=mjasdjkahjasjdsklasdhkalsdkjlsdalja18dasdsdasdasdada
jwt.expiration=86400000

# =============================================
# Firebase Cloud Messaging (FCM)
# L?y t?i: Firebase Console ? Project Settings ? Cloud Messaging
# =============================================
fcm.server-key=fjpoapworuu3-ía-diw-9diu-?ái-asdiadasd9asfasdxrdadasdasdsad

# =============================================
# Email (SMTP) ? M?c 2.8
# B?t: app.mail.enabled=true + ?i?n thông tin Gmail
#
# H??ng d?n t?o App Password Gmail:
#   1. Google Account ? B?o m?t ? Xác minh 2 b??c (b?t tr??c)
#   2. Google Account ? B?o m?t ? App Passwords
#   3. Ch?n "Mail" và "Windows Computer" ? Generate
#   4. Copy m?t kh?u 16 ký t? vào spring.mail.password
# =============================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your-gmail@gmail.com
spring.mail.password=your-16-char-app-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

app.mail.from=your-gmail@gmail.com
# ??t false trong môi tr??ng dev ?? không g?i email th?t
app.mail.enabled=false

# =============================================
# Scheduling
# =============================================
spring.task.scheduling.pool.size=5

# =============================================
# Logging
# =============================================
logging.level.org.springframework.security=INFO
logging.level.com.project=DEBUG
logging.level.org.springframework.mail=DEBUG